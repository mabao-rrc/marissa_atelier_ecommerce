<%# 
  app/views/layouts/application.html.erb
  This is the main layout for the Marissa Atelier e-commerce site.
  It includes global styles (Google Fonts, Bootstrap), scripts, and
  yields page-specific content.
%>

<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Marissa Atelier" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Favicon and Apple touch icon %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Load Google Font: Cormorant Garamond %>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%# Load Bootstrap CSS (v5.3.0) from CDN %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" %>

    <%# Load custom Rails application stylesheet %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container mt-3">
      <%# Flash messages for success (notice) and errors (alert) %>
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <%# Auth links - Sign Up, Log In, Log Out %>
      <div class="d-flex justify-content-end align-items-center mb-3 gap-2">
        <% if customer_signed_in? %>
          <span class="text-muted me-2">Welcome, <%= current_customer.first_name.presence || current_customer.email %>!</span>
          <%= button_to 'Log Out', destroy_customer_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm" %>
        <% else %>
          <%= link_to 'Sign Up', new_customer_registration_path, class: "btn btn-outline-primary btn-sm" %>
          <%= link_to 'Log In', new_customer_session_path, class: "btn btn-outline-success btn-sm" %>
        <% end %>
      </div>
    </div>

    <%# This is where the content of individual views will be inserted %>
    <%= yield %>

    <%# Load Bootstrap JS Bundle (for dropdowns, modals, etc.) %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" %>

    <%# ✅ Debug: Confirm Turbo/UJS is active %>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        console.log("Turbo/UJS loaded");
      });
    </script>
  </body>
</html>
