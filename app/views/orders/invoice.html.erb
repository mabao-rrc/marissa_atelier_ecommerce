<%# ------------------------------------------------------ %>
<%# Invoice Summary – Feature 3.1.3 Step 2                %>
<%# Shows tax and total after checkout                   %>
<%# ------------------------------------------------------ %>

<h1 class="mb-4" style="font-family: 'Cormorant Garamond', serif; color: #896629;">Invoice Summary</h1>

<table class="table">
  <thead>
    <tr>
      <th>Product</th>
      <th>Qty</th>
      <th>Price</th>
      <th>Subtotal</th>
    </tr>
  </thead>
  <tbody>
    <% @cart_items.each do |item| %>
      <tr>
        <td><%= item[:product].name %></td>
        <td><%= item[:quantity] %></td>
        <td><%= number_to_currency(item[:product].price) %></td>
        <td><%= number_to_currency(item[:product].price * item[:quantity]) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p><strong>Subtotal:</strong> <%= number_to_currency(@subtotal) %></p>
<% if @gst > 0 %><p><strong>GST:</strong> <%= number_to_currency(@gst) %></p><% end %>
<% if @pst > 0 %><p><strong>PST:</strong> <%= number_to_currency(@pst) %></p><% end %>
<% if @hst > 0 %><p><strong>HST:</strong> <%= number_to_currency(@hst) %></p><% end %>
<p><strong>Total:</strong> <%= number_to_currency(@total) %></p>

<p class="mt-4"><strong>Note:</strong> We'll save this order in the next step.</p>

<%= link_to "Back to Cart", cart_path, class: "btn btn-secondary me-2" %>
